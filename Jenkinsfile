pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Hello build'
            }
        }

        stage('delpoy') {
            steps {
                echo 'Hello deploy'
            }
        }
    }
}
